import LoginCard from "../components/LoginCard";

export default function EmployerLogin() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-blue-50">
      <LoginCard title="Employer Login" role="employer" />
    </div>
  );
}
